package com.webster;

import com.webster.entity.User;
import com.webster.dao.UserRepositoryImpl;
import com.webster.dao.UserRepository;
import com.webster.entity.Wrapper;

import java.io.IOException;
import java.util.ArrayList;
import java.util.List;


public class Main {

    public static void main(String[] args) throws IOException {

        /*
        List listUser = new ArrayList<User>();

        listUser.add(new User("Arthur", 1000));
        listUser.add(new User("Robert", 1000));
        listUser.add(new User("Gérard", 1000));

        UserRepository userService = new UserRepositoryImpl();

        userService.saveUserListAsJson(new Wrapper(listUser));

        userService.readAllUserFromJson().forEach(u -> {
            System.out.println(u);
        });
         */

    }
}